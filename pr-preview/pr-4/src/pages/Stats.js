import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { Chip } from '@/components/common/Chip';
import { SimpleChart } from '@/components/common/SimpleChart';
import { StatCard } from '@/components/common/StatCard';
import { Card } from '@/components/common/Card';
import { useAnalytics } from '@/hooks/useAnalytics';
import { useSettings } from '@/hooks/useSettings';
import { useTrainingLogs } from '@/hooks/useTrainingLogs';
import { formatHours, formatPercent, formatRatio } from '@/utils/format';
export default function StatsPage() {
    const { logs } = useTrainingLogs();
    const { settings } = useSettings();
    const { range, setRange, snapshot } = useAnalytics(logs, settings);
    return (_jsxs("div", { className: "space-y-4", children: [_jsxs("div", { children: [_jsx("h1", { className: "text-3xl font-bold", children: "Stats" }), _jsx("p", { className: "mt-1 text-sm text-[var(--muted)]", children: "Analytics from saved logs only." })] }), _jsxs(Card, { className: "space-y-3", children: [_jsx("div", { className: "flex flex-wrap gap-2", children: [
                            { label: '7 days', value: '7d' },
                            { label: '30 days', value: '30d' },
                            { label: 'All time', value: 'all' },
                            { label: 'Custom', value: 'custom' },
                        ].map((option) => (_jsx(Chip, { active: range.preset === option.value, onClick: () => setRange({ preset: option.value }), children: option.label }, option.value))) }), range.preset === 'custom' ? (_jsxs("div", { className: "grid gap-3 md:grid-cols-2", children: [_jsx("input", { className: "input-base", type: "date", value: range.start ?? '', onChange: (e) => setRange({ ...range, start: e.target.value }) }), _jsx("input", { className: "input-base", type: "date", value: range.end ?? '', onChange: (e) => setRange({ ...range, end: e.target.value }) })] })) : null] }), _jsxs("div", { className: "grid gap-3 md:grid-cols-4", children: [_jsx(StatCard, { label: "Weekly Goal", value: formatPercent(snapshot.weeklyGoalProgress), helper: `${snapshot.currentStreak} current streak` }), _jsx(StatCard, { label: "Annual Progress", value: formatPercent(snapshot.annualProgress), helper: `${snapshot.bestStreak} best streak` }), _jsx(StatCard, { label: "Gi Split", value: `${snapshot.summary.giCount} / ${snapshot.summary.noGiCount}`, helper: "Gi / No-Gi" }), _jsx(StatCard, { label: "Avg Cardio", value: snapshot.averageCardio.toFixed(1), helper: `Intensity ${snapshot.averageIntensity.toFixed(1)}` })] }), _jsxs("div", { className: "grid gap-3 md:grid-cols-3", children: [_jsx(StatCard, { label: "Sessions", value: `${snapshot.summary.sessions}`, helper: "Selected range" }), _jsx(StatCard, { label: "Total Hours", value: formatHours(snapshot.summary.totalMatHours), helper: `Rolling ${formatHours(snapshot.hoursRolling)}` }), _jsx(StatCard, { label: "Rolls / Session", value: snapshot.rollsPerSession.toFixed(1), helper: `Rounds ${snapshot.totalRounds}` })] }), _jsxs("div", { className: "grid gap-3 md:grid-cols-4", children: [_jsx(StatCard, { label: "Submissions", value: `${snapshot.summary.submissions}`, helper: `Avg ${(snapshot.summary.sessions ? snapshot.summary.submissions / snapshot.summary.sessions : 0).toFixed(1)}` }), _jsx(StatCard, { label: "Taps", value: `${snapshot.summary.taps}`, helper: `Avg ${(snapshot.summary.sessions ? snapshot.summary.taps / snapshot.summary.sessions : 0).toFixed(1)}` }), _jsx(StatCard, { label: "Sub/Tap Ratio", value: formatRatio(snapshot.summary.subTapRatio), helper: "Higher is better" }), _jsx(StatCard, { label: "Injuries", value: `${snapshot.injuryCount}`, helper: `${snapshot.techniquesLearnedCount} techniques logged` })] }), _jsxs("div", { className: "grid gap-3 md:grid-cols-2", children: [_jsx(SimpleChart, { title: "Sessions per week", data: snapshot.sessionsPerWeek }), _jsx(SimpleChart, { title: "Rolling time over time", data: snapshot.rollingTimeTrend }), _jsx(SimpleChart, { title: "Submissions vs taps", data: snapshot.submissionsVsTaps, comparison: true }), _jsx(SimpleChart, { title: "Gi vs No-Gi distribution", data: snapshot.giDistribution })] }), _jsxs(Card, { className: "grid gap-3 md:grid-cols-4", children: [_jsxs("div", { children: [_jsx("div", { className: "text-xs uppercase tracking-[0.18em] text-[var(--muted)]", children: "Sweeps" }), _jsx("div", { className: "text-2xl font-bold", children: snapshot.sweeps })] }), _jsxs("div", { children: [_jsx("div", { className: "text-xs uppercase tracking-[0.18em] text-[var(--muted)]", children: "Escapes" }), _jsx("div", { className: "text-2xl font-bold", children: snapshot.escapes })] }), _jsxs("div", { children: [_jsx("div", { className: "text-xs uppercase tracking-[0.18em] text-[var(--muted)]", children: "Passes" }), _jsx("div", { className: "text-2xl font-bold", children: snapshot.passes })] }), _jsxs("div", { children: [_jsx("div", { className: "text-xs uppercase tracking-[0.18em] text-[var(--muted)]", children: "Takedowns" }), _jsx("div", { className: "text-2xl font-bold", children: snapshot.takedowns })] })] })] }));
}
